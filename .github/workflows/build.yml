name: Build
on:
  workflow_dispatch:
  pull_request:
  # push:
  #   paths:
  #     - config/**
  #     - build.yaml

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@b66a8cc8c1dc2992bdc17ee2e839e08b599190b1 # v0.3.0
