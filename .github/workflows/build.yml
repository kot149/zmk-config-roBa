name: Build
on:
  workflow_dispatch:
  pull_request:
  # push:
  #   paths:
  #     - config/**
  #     - build.yaml

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@0331b7d16e80954b807917f9323e59ffc1e3b626 # v0.3.0
